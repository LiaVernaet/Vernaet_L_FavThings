<section id="heroes">
      <!--loop w handlebars-->
    {{#each avatars}}
    <div class="hero-container">
        <a href="/{{name}}">
        <img src="/images/{{avatar}}" alt="avatar">
        
        </a>
    <h3 class="hero-name">{{name}}</h3>
</div>
    
    {{/each}} <!--close loop-->
     
</section>>